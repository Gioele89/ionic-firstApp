// inject ngRoute for all our routing needs
/*angular.module('todoRoutes',['ngRoute'])

// configure our routes
.config(function($routeProvider, $locationProvider){

	/*$routeProvider

	//route for the home page
	.when('')*/

//});